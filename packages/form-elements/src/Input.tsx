import React from 'react';
import { Form } from 'semantic-ui-react';
import { RelateByUIInputPropTypes } from '.';

const RelateByUIInput = (props: RelateByUIInputPropTypes) => <Form.Input {...props} />;

export default RelateByUIInput;
