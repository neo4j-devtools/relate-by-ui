import React from 'react';
import { Form } from 'semantic-ui-react';
import { RelateByUISelectPropTypes } from '.';

const RelateByUISelect = (props: RelateByUISelectPropTypes) => <Form.Select {...props} />;

export default RelateByUISelect;
