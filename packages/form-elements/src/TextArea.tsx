import React from 'react';
import { Form } from 'semantic-ui-react';
import { RelateByUITextAreaPropTypes } from '.';

const RelateByUITextArea = (props: RelateByUITextAreaPropTypes) => <Form.TextArea {...props} />;

export default RelateByUITextArea;
