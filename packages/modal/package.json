{
  "name": "@relate-by-ui/modal",
  "version": "0.0.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "dependencies": {
    "@emotion/core": "^10.0.10",
    "@emotion/styled": "^10.0.10",
    "@relate-by-ui/buttons": "^0.0.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.6",
    "jest": "^24.5.0",
    "jest-dom": "^3.1.3",
    "react-testing-library": "^6.0.1",
    "ts-jest": "^24.0.0"
  },
  "peerDependencies": {
    "@relate-by-ui/buttons": "*",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "semantic-ui-react": "^0.86.0"
  },
  "scripts": {
    "tsc": "tsc",
    "watch": "tsc -w",
    "test": "jest --no-cache --coverage --config jest.config.js",
    "dev": "jest --no-cache --config jest.config.js --watch",
    "preparePublish": "echo '//registry.npmjs.org/:_authToken=${NPM_TOKEN}' > .npmrc",
    "prepublishOnly": "npm run preparePublish",
    "postpublish": "rm -f .npmrc"
  },
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "lib"
  ]
}
